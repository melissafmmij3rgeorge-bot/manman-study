# 满满大闯关静态发布包

这是可直接部署到 GitHub Pages、Vercel 或 Netlify 的静态网页包。

## GitHub Pages

1. 新建一个公开 GitHub 仓库。
2. 将本目录中的全部文件上传到仓库根目录，保留 `ip/` 文件夹。
3. 在仓库的 Settings → Pages 中选择从 `main` 分支的根目录发布。
4. 等待发布完成后，用 GitHub Pages 地址打开网页。

不要把腾讯云密钥或其他密码上传到仓库。

